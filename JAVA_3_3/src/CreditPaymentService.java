public class CreditPaymentService {
    public int calculate(int percent, int credit) {
        int credit = 1_000_000; // сумма кредита в рублях
        float percent = 9.9; // процентная ставка в год
        float percentMonth = perсent / longTime; // процентная ставка в месяц

        int payment = credit * coeffitsiyentPayment; // расчет аутентного платежа
        float coeffitsiyentPayment = (percentMonth * pow) / (pow - 1);
        float pow = Math.pow(1 + percentMonth, longTime); // возведение в степень

        return payment;

    }
}
